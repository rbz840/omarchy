# Omarchy ARM64 Pi 5 — modules loaded at boot.
# vc4/v3d (KMS+GPU) are auto-probed by DT; list optional but useful ones here.

# Broadcom VideoCore VI (GPU) + KMS
vc4
v3d

# RP1 peripherals (USB3, SDIO, Ethernet) — probed via DT, kept explicit
rp1
bcm2712-g

# V4L2 codecs (HEVC/H264 hardware decode via request API)
rpivid
rp1_cfe
bcm2835-codec

# Bluetooth (CYW43455)
hci_uart
btbcm
